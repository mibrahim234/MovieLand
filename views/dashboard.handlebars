<div class="review-page mt-6" >
      <div class="columns is-centered">
        <div class="column is-5-tablet is-5-desktop is-5-widescreen">

<ol class="post-list column">
  {{#each posts}}
  <li>
    <article class="post">
      <div class="title">
        <h2>{{title}}</h2>
        <h3>{{review}}</h3>
      </div>
      <div class="meta">
      </div>
    </article>
  </li>
  {{/each}}
</ol>
 </div>
  </div>
</div>

{{#if loggedIn}}
<section class="new-post">
  <div class="hero-body">
    <div class="post-form container mt-6">
      <div class="columns is-centered">
        <div class="column is-4-tablet is-4-desktop is-5-widescreen">
  <form class="box new-post-form">
  <div class="field">

          <label class="newpost">Create New Post</label>
    <label class="label">Title</label>
    <div class="control">
      <input class="input" type="text" id="title" name="title" placeholder="Enter a title">
    </div>
  </div>

  <div class="field">
    <label class="label">Review</label>
    <div class="control">
      <textarea class="textarea" class="input" id="review" name="review" placeholder="This movie is amazing!"></textarea>
    </div>
  </div>

  <button type="submit" class="button is-info">Create</button>
</form>
 </div>
  </div>
   </div>
  </div>
{{/if}}

{{!-- {{#if posts.length}}
<section class="your-posts">
  <h2>Your Posts</h2>
  <ol>
    {{#each posts as |post|}}
    <li>
      {{> post-info post}}
      <a href="/dashboard/edit/{{post.id}}" class="edit-link">Edit post</a>
    </li>
    {{/each}}
  </ol>
</section>
{{/if}} --}}

{{!-- <script src="/javascript/omdb.js"></script> --}}
{{#if loggedIn}}
<script src="/javascript/add-post.js"></script> 
{{!-- nice post man! --}}
{{/if}}